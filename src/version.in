#define FILEVER			$MajorVersion$,$MinorVersion$,$MicroVersion$,$WCREV$
#define PRODUCTVER		$MajorVersion$,$MinorVersion$,$MicroVersion$,$WCREV$
#define STRFILEVER		"$MajorVersion$, $MinorVersion$, $MicroVersion$, $WCREV$\0"
#define STRPRODUCTVER	"$MajorVersion$, $MinorVersion$, $MicroVersion$, $WCREV$\0"

#define SVNPROT_VERMAJOR			$MajorVersion$
#define SVNPROT_VERMINOR			$MinorVersion$
#define SVNPROT_VERMICRO			$MicroVersion$
#define SVNPROT_VERBUILD			$WCREV$
#define SVNPROT_VERDATE				"$WCDATE$"

#ifdef _WIN64
#define SVNPROT_PLATFORM			"64 Bit $DEVBUILD$"
#else
#define SVNPROT_PLATFORM			"32 Bit $DEVBUILD$"
#endif

